import { Metadata } from "next";

export const generateMetadata = (): Metadata => {
  return {
    title: "Home - GMN"
  };
}

const IndexPage = () => {
  return (
    <>
    </>
  );
}

export default IndexPage;